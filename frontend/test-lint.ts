// Test file with intentional formatting issues
const badlyFormatted = 'test';
const unusedVariable = 'this should trigger linting';

function poorlyFormatted(param1: string, param2: number) {
  return param1 + param2;
}

export default poorlyFormatted;
