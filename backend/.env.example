# ==============================
# Application Config
# ==============================
# Port where the backend server will run
PORT=8000

# Runtime environment: development | production | test
NODE_ENV=development

# Application name (used in logs, monitoring, etc.)
APP_NAME=Space Rep


# ==============================
# Database (PostgreSQL + Prisma)
# ==============================
# Main database connection URL for Prisma ORM
# Replace <password> with your real DB password
DATABASE_URL=postgresql://user:password@localhost:5432/study_tool?schema=public

# Direct database connection (bypasses pooling, used for migrations)
DIRECT_DATABASE_URL=postgresql://postgres:password@localhost:5432/study_tool?schema=public


# ==============================
# OpenRouter API (AI Integration)
# ==============================
# API key for OpenRouter (used for AI-related features/integration)
# Sign up at https://openrouter.ai/ and generate an API key
OPENROUTER_API_KEY=your_openrouter_api_key


# ==============================
# Authentication (JWT)
# ==============================
# Secret key for signing JWT access tokens (must be long & random in prod)
JWT_SECRET=your-jwt-secret-here-minimum-32-characters

# Expiry duration for access tokens (e.g., 15m = 15 minutes)
JWT_ACCESS_EXPIRY=15m

# Secret key for signing JWT refresh tokens (different from JWT_SECRET)
JWT_REFRESH_SECRET=your-refresh-secret-here-minimum-32-characters

# Expiry duration for refresh tokens (e.g., 7d = 7 days)
JWT_REFRESH_EXPIRY=7d

# Number of bcrypt salt rounds for password hashing (higher = more secure but slower)
BCRYPT_ROUNDS=12


# ==============================
# CORS Configuration
# ==============================
# Comma-separated list of allowed origins (e.g., http://localhost:3000,http://localhost:3001)
ALLOWED_ORIGINS=http://localhost:3000
