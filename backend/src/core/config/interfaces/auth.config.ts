import { AuthConfig, JwtConfig } from './config.interface';

// This file can be used to export the interfaces again if needed elsewhere,
// but the primary definition is in config.interface.ts.
export { AuthConfig, JwtConfig };
